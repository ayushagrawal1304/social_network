<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="sign_up">
		<h1 align="center"> <%= @community.name %> </h1>
	</div>
</nav>

<div class="container">
  <div class="sign_up">

		<%= form_with model: @approval, url: users_community_invite_path(@community.id) do |user| %>

			<div class="form-group">
			
		   <br> <%= select_tag(:user_id, options_for_select(invite_user_options), { class: 'form-control'}) %></br>
		 <br><div class="actions">
		  	<%= user.submit "Invite", class: "btn btn-primary"%>
		  </div></br>

		<% end %>

	</div>
</div>
